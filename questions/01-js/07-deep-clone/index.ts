export function deepClone<T>(value: T): T {
  // TODO 1: If value is null or not an object/array (primitive), return it directly
  // TODO 2: If value is an Array, map each element through deepClone recursively
  // TODO 3: If value is a plain object, create a new object and recursively clone each own property
  // TODO 4: Return the cloned structure with the same type as the input
  throw new Error('Not implemented');
}
